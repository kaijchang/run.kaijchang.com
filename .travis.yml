language: node_js
node_js: 12
script:
- yarn build
cache:
  directories:
  - .cache
  - public
deploy:
  provider: pages
  skip-cleanup: true
  github-token: "$GITHUB_TOKEN"
  local_dir: public
  on:
    branch: master
